apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: crucible

bases:
  - ../../base

patchesStrategicMerge:
  - patch-services.yaml

generatorOptions:
  disableNameSuffixHash: true
